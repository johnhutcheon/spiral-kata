const {
  spiralGenerator,
  newVertical,
  newHorizontal,
  newDiagonal_Left,
  newDiagonal_Right,
} = require("../spiral");

describe("spiralGenerator", () => {
  test("the function generates the correct numbers for each direction array, when pushing the next value", () => {
    expect(spiralGenerator(horizontal));
  });
});
